배열은 []
console.log(이름[2]) - 0번째부터 세서 두번째
객체리터럴 { : }
console.log(이름.이름)
console.log(이름['이름'])

i++ -> 나중에 더해줘
++ㅑ -> 지금 더해서 보여줘

let nu = -2;
nu = -nu; 같이 재할당 가능한데, 앞에 아무것도 안씀.

for(){
    안에 console없이 sum += i; 이래도됨.
} 
그리고 밖에서 console.로 확인해보기.


함수
함수호출 펑션 없이 맨땅에헤딩()!! -> 함수이름();
파라미터 - 함수의 변수
return 만 쓰면 안나옴. -> 콘솔로 꺼내주기. console.log(name(파라미터))